# Core
flask==3.1.1
python-dotenv==1.1.0

# LangChain (modern & stable)
langchain==0.3.26
langchain-core==0.3.66
langchain-community==0.3.26

# Vector DB
langchain-pinecone==0.2.8

# Embeddings & PDF
sentence-transformers==4.1.0
pypdf==5.6.1

# Gemini (DIRECT SDK — NO LANGCHAIN ADAPTER)
google-generativeai==0.7.2

# Editable install (ONLY if setup.py exists)
-e .
